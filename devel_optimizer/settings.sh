#######################################################################
######################## Customization Options ########################
#######################################################################

# User interface of choice (cli, dialog)
export USER_INTERFACE='cli'

# Editor used to modify settings (vim, nano, emacs, micro)
# The EDITOR environment variable takes precedence if already set
export SETTINGS_EDITOR='micro'

# Country to generate the mirror list for (if applicable)
export MIRRORLIST_COUNTRY='United States'

