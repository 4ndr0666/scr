Small ISO which can be used to install the latest stable 4MLinux via Ethernet.

1. Boot net-install.iso
2. Perform the installation to a hard disk drive
3. Reboot
4. Run the "zk update" update command

Both BIOS and UEFI are supported
Filesystems to choose: btrfs ext2 ext3 ext4 jfs reiserfs xfs
